export interface GuestFormItem {
  label: string;
  htmlId: string;
  formControlName: string;
  labelId: string;
  placeholder: string;
  items: string[];
}

export interface DatePickerItem {
  label: string;
  htmlId: string;
  formControlName: string;
  placeholder: string;
}
